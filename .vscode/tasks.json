// A task runner configuration.
{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gulp: default",
            "command": "gulp",
            "problemMatcher": [
                "$gulp-tsc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}